plugins {
    id 'java-library'
}

dependencies {
    // Note: Use 'api' and 'implementation' in here -- the latter is passed on transitively.
    
    testImplementation 'org.junit.jupiter:junit-jupiter:5.5.2'
    testImplementation 'org.hamcrest:hamcrest:2.2'
}

test {
    useJUnitPlatform()
}
